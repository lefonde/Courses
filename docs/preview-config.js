/* Isolated pilot: never uses the regular site's progress storage key. */
window.STUDY_CONFIG = Object.freeze({mode:'static',storageKey:'matzpen:22902:2026-s3:learning-preview:v1',learningPilot:true,reportImport:true,preview:true});
